ALTER TABLE celebs
ADD COLUMN
twitter_handle TEXT;

SELECT * FROM CELEBS;
-- Added a column to the table called twitter_handle that takes in only text inputs, but all of the values in that column are null.
